let arr = [1, 2, 3, 4];

// Use map to create an array with the square of each number
let squaredNumbers = arr.map(el => el ** 2);

// Print the squared numbers
squaredNumbers.forEach(el => console.log(el));

